export default class Item {
    constructor(length, width, height, x, y, z, name, color, stackable) {
        this.length = length;
        this.width = width;
        this.height = height;
        this.x = x;
        this.y = y;
        this.z = z;
        this.name = name;
        this.color = color;
        this.stackable = stackable;
    }
}
